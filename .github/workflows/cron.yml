name: Cron

on:
  schedule:
    - cron: "*/5 * * * *"

permissions:
  contents: read

jobs:
  build:
    name: CI
    runs-on: ubuntu-latest

    steps:
      - name: Hello
        run: echo "Hello world"
